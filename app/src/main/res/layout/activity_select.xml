<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_select"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="at.fh.swengb.resifo_android.SelectActivity">

    <Button
        android:text="anzeigen"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:id="@+id/button_bearbeiten"
        android:layout_marginTop="50dp"
        android:minHeight="60dp"
        android:background="#b01817"
        android:textColor="#ffffff"
        android:textSize="24sp"
        android:maxWidth="60dip"
        android:layout_below="@+id/button_neu"
        android:layout_alignStart="@+id/button_neu"
        android:onClick="editForm" />

    <Button
        android:text="wichtige information"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/button_information"
        android:minHeight="60dp"
        android:textSize="24sp"
        android:maxWidth="60dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:onClick="getInfo" />

    <Button
        android:text="neu"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:id="@+id/button_neu"
        android:layout_marginTop="80dp"
        android:minHeight="60dp"
        android:background="#b01817"
        android:textColor="#ffffff"
        android:textSize="24sp"
        android:maxWidth="60dip"
        android:onClick="newForm"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
